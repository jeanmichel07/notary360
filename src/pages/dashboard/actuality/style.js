import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  content: {},
  title: {
    fontWeight: "bolder",
    fontSize: 12,
    marginTop: 4,
  },
  description: {
    marginBottom: 10,
    fontSize: 10,
    marginTop: 4,
  },
});

export default useStyles;
